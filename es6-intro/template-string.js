const priya = 'Asif Akbar';
const meye = 'Ayub Bacchu';
const kobita = `James`;

const multiline = `this is multiline
this is seecond line
this is third line`;
console.log(multiline);

const count = 5;

const new1 = `hello my friend-${count}`;
console.log(new1);

const first = 'Mamun';
const last = 'chowdhury';
const fullName = first + '' + last;
const fullNew = `This person name is ${first} ${last}`;

console.log(fullNew);
